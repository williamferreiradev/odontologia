{
  "name": "odontologia-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --prefix apps/web",
    "install:web": "npm install --prefix apps/web"
  },
  "devDependencies": {
    "next": "14.2.35"
  }
}
